Selenium chrome driver should be placed in /opt/selenium/
Add /opt/selenium to PATH
Place driver.py and credits.html in /opt/myview
Install required python packages:
   pip install -r requirements.txt
